// should have NO strategy (a chain died before reaching a good place)
.name "overallBad0.apt"
.type LPN
.options
winningCondition="A_REACHABILITY"

.places
env[env="true"]
env1[env="true",reach="true"]
sa1
sa2
sb1
sb2[reach="true"]

.transitions
t1[tfl="{sa1}->{sa2}"]
t2
tenv

.flows
tenv: {env} -> {env1}
t1: {sa1} -> {sa2,sb1}
t2: {sb1} -> {sb2}
.initial_marking {sa1,env}
